{% extends 'base.html.twig' %}

{% block title %}Zona{% endblock %}

{% block body %}
    <h1>Zona</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zona.id }}</td>
            </tr>
            <tr>
                <th>NombreZona</th>
                <td>{{ zona.nombreZona }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zona_index') }}">back to list</a>

    <a href="{{ path('app_zona_edit', {'id': zona.id}) }}">edit</a>

    {{ include('zona/_delete_form.html.twig') }}
{% endblock %}
