{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
    <h1>Equipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipo.id }}</td>
            </tr>
            <tr>
                <th>NombreEquipo</th>
                <td>{{ equipo.nombreEquipo }}</td>
            </tr>
            <tr>
                <th>Presupuesto</th>
                <td>{{ equipo.presupuesto }}</td>
            </tr>
            <tr>
                <th>Fecha Fundación</th>
                <td>{{ equipo.fechaFunda ? equipo.fechaFunda|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipo_index') }}">back to list</a>

    <a href="{{ path('app_equipo_edit', {'id': equipo.id}) }}">edit</a>

    {{ include('equipo/_delete_form.html.twig') }}
{% endblock %}
