<form method="post" action="{{ path('app_jugador_delete', {'id': jugador.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jugador.id) }}">
    <button class="btn">Delete</button>
</form>
